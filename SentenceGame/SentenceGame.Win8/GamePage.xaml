<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="SentenceGame.Win8.GamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SentenceGame.Win8"
    xmlns:common="using:SentenceGame.Win8.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding GamePageViewModel, Mode=TwoWay, Source={StaticResource Locator}}"
    >

    <Page.Resources>

        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">Przetłumacz następujące zdanie:</x:String>

        <DataTemplate x:Key="ItemDataTemplateTranslation">
            <StackPanel>
                <Button Content="{Binding}" Command="{Binding ElementName=pageRoot, Path=DataContext.SelectCommand}" CommandParameter="{Binding}" FontSize="40"/>
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="ItemDataTemplateSelTranslation">
            <StackPanel>
                <Button Content="{Binding}" Command="{Binding ElementName=pageRoot, Path=DataContext.SelectRevCommand}" CommandParameter="{Binding}" FontSize="40"/>
            </StackPanel>
        </DataTemplate>
        <ItemsPanelTemplate x:Key="ItemsPanelTemplate">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" />
        </ItemsPanelTemplate>
    </Page.Resources>


    <Grid Style="{StaticResource LayoutRootStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Click="GoBack" IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}" Style="{StaticResource BackButtonStyle}"/>
            <TextBlock x:Name="pageTitle" Grid.Column="1" Text="{StaticResource AppName}" Style="{StaticResource PageHeaderTextStyle}"/>
        </Grid>
        <TextBlock HorizontalAlignment="Center" Margin="200,150,200,0" Grid.Row="1" TextWrapping="Wrap" Text="{Binding Sentence.Text}" VerticalAlignment="Top" Height="91" Width="983" FontSize="50" TextAlignment="Center" />
        <ItemsControl Margin="100,300,100,500" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Grid.Row="1" 
                      ItemsSource="{Binding SelTranslation}" 
                      ItemsPanel="{StaticResource ItemsPanelTemplate}" 
                      ItemTemplate="{StaticResource ItemDataTemplateSelTranslation}" />
        <TextBlock Grid.Row="1" Text="{Binding Answer}" Margin="0,0,0,-100" VerticalAlignment="Center" Height="60" HorizontalAlignment="Center" FontSize="30"/>
        <ItemsControl Margin="100,300,100,100" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Grid.Row="1" 
                      ItemsSource="{Binding Translation}" 
                      ItemsPanel="{StaticResource ItemsPanelTemplate}" 
                      ItemTemplate="{StaticResource ItemDataTemplateTranslation}" />

        <VisualStateManager.VisualStateGroups>

            <!-- Visual states reflect the application's view state -->
            <VisualStateGroup x:Name="ApplicationViewStates">
                <VisualState x:Name="FullScreenLandscape"/>
                <VisualState x:Name="Filled"/>

                <!-- The entire page respects the narrower 100-pixel margin convention for portrait -->
                <VisualState x:Name="FullScreenPortrait">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PortraitBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>

                <!-- The back button and title have different styles when snapped -->
                <VisualState x:Name="Snapped">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="pageTitle" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedPageHeaderTextStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</common:LayoutAwarePage>